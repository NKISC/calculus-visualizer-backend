"""Integral computation placeholder."""

from typing import Any


def compute_integral(
    expression: Any,
    variable: str,
    lower_bound: float | None = None,
    upper_bound: float | None = None,
) -> Any:
    """Compute a future symbolic or definite integral.

    TODO: Implement with SymPy after integration request rules are finalized.
    """

    raise NotImplementedError("Integral computation is not implemented yet.")
